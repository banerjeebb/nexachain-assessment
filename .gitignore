# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# dependencies (covered per-package, kept here as a safety net)
node_modules/

# logs
*.log
npm-debug.log*

# env files — never commit secrets; templates are explicitly allowed
.env
.env.local
.env.*.local
!.env.example
